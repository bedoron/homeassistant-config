# Loads default set of integrations. Do not remove.
default_config:

# Load frontend themes from the themes folder
frontend:
  themes: !include_dir_merge_named themes

# Text to speech
tts:
  - platform: google_translate

http:
  server_port: 80
  use_x_forwarded_for: true
  trusted_proxies:
    - 192.168.14.5
  ip_ban_enabled: true
  login_attempts_threshold: 5

automation: !include automations.yaml
script: !include scripts.yaml
scene: !include scenes.yaml
group: !include groups.yaml
climate: !include climate.yaml
switch: !include switch.yaml
rest: !include rest.yaml
#light: !include light.yaml

lovelace:
  mode: storage
  dashboards:
    docker-health:
      mode: yaml
      title: Docker Health
      icon: mdi:docker
      show_in_sidebar: true
      filename: docker-health-dashboard.yaml

homeassistant:
  name: Home
  latitude: !secret home_latitude
  longitude: !secret home_longitude
  elevation: 17.10
  unit_system: metric
  time_zone: Asia/Jerusalem

google_assistant: !include google_assistant_integ.yaml

# Telegram bot migrated to a config entry (UI-configured) 2026-04.
# The previous `telegram_bot:` YAML block and `notify: - platform: telegram`
# were removed for HA 2026.8.0 compatibility. Automations now call
# `notify.send_message` targeting `notify.home_pignonymous`.

influxdb:
  api_version: 1
  host: victoriametrics.local
  port: 80
  max_retries: 3
  measurement_attr: entity_id
  tags_attributes:
    - friendly_name
    - unit_of_measurement
  include:
    domains:
      - binary_sensor
      - climate
      - counter
      - cover
      - light
      - sensor
      - switch
